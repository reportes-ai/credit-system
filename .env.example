# Configuración de Base de Datos MySQL compartida
# Copia este archivo como .env y completa con tus credenciales
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=credit_system

# Puerto del servidor
PORT=3000

# Clave secreta JWT (cambia esto en producción)
JWT_SECRET=credit_system_secret_2024
